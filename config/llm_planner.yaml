# LLMPlanner 配置（T3.5）
# v0.9.1 已用 DeepSeek 完成真实 LLM Agent 验证。
# 演示/生产环境可将 enabled 设为 true，并通过环境变量 LLM_API_KEY 传入密钥。
enabled: false
provider: openai-compatible
base_url: https://api.deepseek.com/v1
model: deepseek-chat
api_key_env: LLM_API_KEY
max_tokens: 1000
temperature: 0.2
timeout_s: 30
